# staging で確認待ちの変更

staging（テスト用のサイト）には入っていて、main（本番 https://orca-map.vercel.app）にはまだ入っていない変更と、
本番に出す前に触って確かめたいことの一覧です。書き方と更新のタイミングは [CLAUDE.md](CLAUDE.md) にあります。

## 先にやること

- [ ] `CLAUDE.md`（このリストを毎回書く決まり）を main に入れる → https://github.com/tanha0327/orca-type/pull/51 をマージする。
  新しい作業は main から始まるので、main に入るまでは次の作業でこの決まりが守られない
- [x] `supabase/sql/006_feed_folders.sql` は実行済み（2026-09-26。これで staging でもフォルダ機能を確かめられる）
- [x] `supabase/sql/005_x_verification.sql` は実行済み（2026-09-26 に Supabase で確認）

## 確認待ち

### いろいろな自作キーボードに対応（13 機種の組み込みと配列の取り込み）

`claude/loving-tesla-4ys2co` · 2026-09-26

盤面・打鍵の読み替え・書き出しを「キーボード定義」から組み立てる作りに変え、Orca echo のほかに 12 機種を組み込んだ。
QMK / VIA / KLE / ZMK の配列データも取り込める。保存データの形が変わる（v2 → v3）ので、いちばん影響が大きい。

- [ ] 本番で作った配列が引き継がれる: 本番で JSON を書き出して staging で読み込み、レイヤー・コンボ・エンコーダー・
  左右のパッド・トラックボールの割当がそのまま出る（本番と staging はブラウザの保存データを共有しないので、JSON で試す）
- [ ] Orca echo の盤面の見た目が本番と変わらない（キーの位置・文字の大きさ・エンコーダー・パッド・トラックボール）
- [ ] 「キーボードを変える」で 12 機種に切り替えて盤面が崩れない。回転した親指キーのある ErgoDox EZ・Kyria・Corne は特に
- [ ] 別のキーボードに切り替えて Orca echo に戻ると、前の配列が残っている
- [ ] Orca 以外のキーボードで入力キャプチャ ON にして、文字キーとレイヤーキー（無変換・変換など）が HUD に正しく出る
- [ ] レイヤーを足す・消す（最大 16 枚）。キーのピッカーに、無いレイヤーへのレイヤーキーが出ない
- [ ] 「配列を取り込む」で QMK の info.json / VIA の定義 / KLE の Raw data / ZMK の physical layout を貼って盤面ができる。
  「定義を JSON で保存」も動く
- [ ] 書き出しで ZMK 風と QMK keymap.c 風を切り替えられ、コンボの key-positions がキーの順番と合っている
- [ ] みんなの配列で、Orca 以外のキーボードの投稿がその機種の盤面で出る。自分と違うキーボードの投稿には一致度が出ない
- [ ] DB が共通なので、staging から投稿すると本番のみんなの配列にも出る。staging からの投稿は新しい形で保存され、
  本番の古いコードは Orca echo の盤面で描くため、エンコーダー・パッドの割当が空になり、Orca 以外は盤面が合わないはず。
  試しに投稿したら本番のサイトでも見え方を確かめ、要らなければ消す

### みんなの配列に Mac / Windows の OS タグ

`claude/nifty-fermi-e2vi8e` · 2026-09-26

投稿時に OS を選べ、タイムラインを OS で絞り込める。タグは配列の JSON に入るので DB の変更は無し。

- [ ] 投稿時に 🍎 Mac / 🪟 Windows / 指定しない を選べ、初期値がいま使っているパソコンの OS になっている
- [ ] 投稿カード・拡大モーダル・保存画像に OS のタグが出る
- [ ] タイムラインの上の OS の絞り込みが、フォルダを切り替えても重ねてかかる。リロードしても選んだ OS が残る
- [ ] OS で絞り込んでいる間は、マイフォルダの ↑↓（手動の並べ替え）が出ない

### X のポストで本人確認

`claude/beautiful-ramanujan-0qzbyj` · 2026-09-26

確認コード入りのポストを X にして URL を貼ると、投稿・コメントに「✓ 𝕏 @ユーザー名」のバッジが付く。
X の API は使わず、DB の関数が X の公開 oEmbed で確かめる。X に載せるリンクは staging からでも本番の URL にした。

- [ ] プロフィール編集に「本人確認（X のポスト）」の欄があり、確認コードが出る（開き直しても同じコード）
- [ ] ポストの画面が開き、本文に確認コードと本番の URL（https://orca-map.vercel.app）が入っている。staging の URL ではない
- [ ] ポストの URL を貼ると確認でき、自分の投稿・コメント・拡大モーダルにバッジが出る。バッジを押すと X のプロフィールが開く
- [ ] 確認のあとでポストを消しても、バッジが残る
- [ ] 他人のポストや、コードの入っていないポストの URL では確認できない。続けて押すと 10 秒待つよう言われる
- [ ] 本人確認を自分で取り消せる
- [ ] 投稿の「X でシェア」のリンクも本番の URL になっている

### みんなの配列のフォルダ分け・近い順・NEW / 話題と打鍵音

`claude/hopeful-fermi-5tx399` · 2026-09-26

投稿を配列の特徴で自動でフォルダ分けし、自分用のフォルダにも保存できる。並び替えに近い順、NEW と 🔥 の印。
打鍵にスイッチの音を鳴らせる。

- [ ] 投稿が特徴で自動でフォルダ（ホームロー修飾派・Vim 派・コンボ使い・マウス派・多層派・ミニマル・スタンダード）に分かれ、
  投稿時に選び直せる
- [ ] マイフォルダを作る・名前を変える・消す（消しても中の投稿そのものは消えない）。投稿を保存して ↑↓ で並べ替えられる。
  別のアカウントからは見えない
- [ ] 並び替えの 近い順 で自分の配列に近い投稿が上に来る。各投稿に一致度が出る
- [ ] 24 時間以内の投稿に NEW、いいね 10 以上に 🔥 が付き、新しい順では 🔥 が上にまとまる
- [ ] HUD の ⚙ で打鍵音（赤軸・茶軸・青軸）を選ぶとその場で試し鳴らしし、音量も変えられる。初期値はオフ
- [ ] 打鍵音: 長押しの確定・コンボ・ホイール・スワイプでそれぞれ別の音が鳴る。リロードしても設定が残る

### みんなの配列: 各投稿の盤面で比較・並び替え・アイコンのボタン・保存画像

`claude/affectionate-thompson-l2erck` · 2026-09-24

各投稿の盤面で自分の配列と違うキーを見せ、並び替えを増やし、操作ボタンをアイコンにそろえた。保存する画像は横 2 × 縦 4。

- [ ] 各投稿の盤面で、自分の配列と違うキーがピンクになり「あなたと N キー違う」と出る
- [ ] 盤面の下のミニキーマップを押すと、全部の投稿と右上がそろってそのレイヤーに切り替わる
- [ ] 右上に、比べる基準の自分の配列がレイヤーのタブ付きで出る
- [ ] 並び替え（今熱い・人気・新しい順・古い順）が効き、リロードしても選んだ並び順が残る
- [ ] 投稿のタイトルを押すと拡大モーダルが開く
- [ ] コメント・いいね・画像保存・X シェア・削除が同じ大きさのアイコンのボタンで、件数はバッジ。いいねがコメントより前にある
- [ ] 画像保存で 8 レイヤーが横 2 × 縦 4 に並び、投稿者名の下が切れない
- [ ] スマホ幅で、並び替えがタイムラインの上に出て、投稿をタップすると比較のモーダルが開く
